# Mobile client — to be scaffolded

Planned stack: **Expo** (React Native), TypeScript.

Will consume the same Notextra REST API as the web client.

Scaffold command (when ready):

```bash
npx create-expo-app@latest . --template blank-typescript
```
